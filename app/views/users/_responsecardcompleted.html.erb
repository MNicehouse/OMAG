<div class="container">
  <% if @response_completed_exist %>
  <p>completed tests:</p>
  <div class="row row-cols-1 row-cols-md-3 g-4">
    <!--CARD ITERATION -->
    <% responses_completed.each do |response| %>
      <div class="col">
      <a href="<%= user_responses_path( myclient, response, redir_param: 21, accord_param: "flush-headingTwo")%>"  class="nav-link px-0">
        <div class="card h-100">
           <%= image_tag("assessments/#{pic_response_picker()}", width: 200, class: "card-img-top", alt: "assessment picture") %>
          <div class="card-body">
            <h5 class="card-title"></h5>
            <p class="card-text text-truncate"><%= response.assessment.name %></p>
          </div>
          <ul class="list-group list-group-flush">
            <li class="list-group-item">
              <%= response.assessment.description %>
            </li>
            <li class="list-group-item d-flex justify-content-between">
              <p><%= "#{response.assessment.questions_assessments.count} questions totally" %></p>
              <p><%= "#{response.answers.count} questions completed" %></p>
            </li>
          </ul>
        </div>
        </a>
      </div>
    <% end %>
  </div>
  <% else %>
    <p>no completed tests</p>
  <% end %>
</div>